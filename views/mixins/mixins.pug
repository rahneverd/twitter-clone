mixin createTweetForm(userLoggedIn)
  .tweetFormContainer
    .userImageContainer
      img(src=userLoggedIn.profilePic, alt="User's profile picture")
    .textAreaContainer 
      textrea#tweetTextArea(placeholder="What's happening?")
      .buttonsContainer
        button#submitTweetButton(disabled="") Tweet